# Firebase project config
# Set production secrets via `firebase functions:secrets:set KEY`
# MONGODB_URI / JWT secrets are gone — replaced by Firebase Admin SDK.

FIREBASE_PROJECT_ID=assetflow-hackathon
