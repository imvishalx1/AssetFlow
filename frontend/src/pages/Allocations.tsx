import { Placeholder } from '../components/Placeholder';

export function Allocations() {
  return <Placeholder title="Allocations & Transfers" note="Allocate assets, handle 409 double-allocation via Transfer Request modal, process returns." />;
}
