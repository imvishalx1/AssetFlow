import { Placeholder } from '../components/Placeholder';

export function Assets() {
  return <Placeholder title="Asset Directory & Registration" note="Register assets (auto-tag AF-0001), search/filter, lifecycle status + history." />;
}
